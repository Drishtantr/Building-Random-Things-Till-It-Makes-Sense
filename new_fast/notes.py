# Terminal commands that I entered:
# Creating and installing the virtual environment: python3 -m venv venv
# Activating the virtual environment: source venv/bin/activate
# Installing the FastAPI library: pip install "fastapi[all]"
# Running the FastAPI server: uvicorn car_sharing:app --reload

# add get_cars(), serve at /api/cars, return the data
